---
# - name: Ensure GitHub deploy key is present on the server
#   copy:
#     content: "{{ ssh_deploy_key }}"
#     dest: ~/.ssh/cloud-assignment-git-repo-key
#     mode: '0600'

# # See: https://stackoverflow.com/a/37096534/100134
# - name: Ensure setfacl support is present.
#   package: name=acl
#   become: true
#   environment: "{{ proxy_env }}"

- name: Make sure that github code repo directory exists
  file:
    path: '~/cluster-and-cloud-computing-assignment-2'
    state: 'directory'

- name: Clone the code repository into home directory
  git:
    repo: "https://{{ githubuser | urlencode }}:{{ githubpassword | urlencode }}@github.com/Bachfischer/cluster-and-cloud-computing-assignment-2.git"
    dest: ~/cluster-and-cloud-computing-assignment-2
  environment: "{{ proxy_env }}"
  become: true

#TODO: Move to docker setup task
- name: Make sure that Docker config directory exists
  become: yes
  file:
    path: '~/.docker'
    state: 'directory'

- name: Ensure Docker client proxy settings are present on the server
  become: yes
  copy:
    content: "{{ docker_proxy_settings }}"
    dest: ~/.docker/config.json

- name: Configure compose
  become: yes
  template:
    src: "{{ playbook_dir }}/../../web/docker-compose.yaml.j2"
    dest: "~/cluster-and-cloud-computing-assignment-2/web/docker-compose.yaml"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"

# Stop existing docker containers and remove them (if any)

- name: Stop Frontend Docker container
  become: yes
  docker_container:
    name: frontend
    state: absent

- name: Stop Backend Docker container
  become: yes
  docker_container:
    name: backend
    state: absent

- name: Run docker compose
  become: yes
  docker_compose:
    project_src: "~/cluster-and-cloud-computing-assignment-2/web/"
    pull: false
    build: yes
    state: present
    remove_orphans: no
    recreate: always